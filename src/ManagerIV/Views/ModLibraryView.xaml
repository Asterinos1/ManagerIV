<UserControl x:Class="ManagerIV.Views.ModLibraryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             xmlns:local="clr-namespace:ManagerIV.Views"
             xmlns:vm="clr-namespace:ManagerIV.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800"
             AllowDrop="True"
             Drop="UserControl_Drop"
             DragOver="UserControl_DragOver">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />
    </UserControl.Resources>
    
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Header and drag-and-drop info -->
        <Grid Grid.Row="0" Margin="0,0,0,15">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <StackPanel Grid.Column="0">
                <TextBlock Text="Mod Library" FontSize="24" FontWeight="Bold" Foreground="{DynamicResource TextFillColorPrimaryBrush}" />
                <TextBlock Text="Drag and drop mod archives (.zip, .rar, .7z) here to import them." FontSize="13" Foreground="{DynamicResource TextFillColorSecondaryBrush}" Margin="0,4,0,0" />
            </StackPanel>
            
            <ui:Button Grid.Column="1" 
                       Icon="{ui:SymbolIcon FolderAdd24}" 
                       Content="Import Archive" 
                       Command="{Binding ImportModArchiveCommand}"
                       VerticalAlignment="Center" />
        </Grid>

        <!-- Library List with Drag-and-Drop Reordering -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="3*" />
                <ColumnDefinition Width="2*" />
            </Grid.ColumnDefinitions>

            <ui:Card Grid.Column="0" Margin="0,0,10,0" Padding="0">
                <ListBox x:Name="LibraryListBox" ItemsSource="{Binding LibraryMods}"
                         ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                         PreviewMouseLeftButtonDown="ListBox_PreviewMouseLeftButtonDown"
                         MouseMove="ListBox_MouseMove"
                         DragOver="ListBox_DragOver"
                         Drop="ListBox_Drop"
                         AllowDrop="True"
                         Background="Transparent"
                         BorderThickness="0">
                    <ListBox.ItemTemplate>
                        <DataTemplate DataType="{x:Type vm:ModViewModel}">
                            <Border BorderThickness="0,0,0,1" BorderBrush="{DynamicResource ControlStrokeColorDefaultBrush}" Padding="10,8" Margin="0" HorizontalAlignment="Stretch">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto" />  <!-- Priority / Enable Checkbox -->
                                        <ColumnDefinition Width="*" />     <!-- Mod Info (Name, Version) -->
                                        <ColumnDefinition Width="Auto" />  <!-- Visual Tags (Type, Compatibility) -->
                                    </Grid.ColumnDefinitions>

                                    <!-- Priority Indicator and Toggle Checkbox -->
                                    <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                                        <TextBlock Text="{Binding Priority, StringFormat='\{0:D3\}'}" FontSize="12" Foreground="{DynamicResource TextFillColorTertiaryBrush}" Width="30" VerticalAlignment="Center" TextAlignment="Center" />
                                        <CheckBox IsChecked="{Binding IsEnabled, Mode=OneWay}" Command="{Binding DataContext.ToggleModEnabledCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" CommandParameter="{Binding}" Margin="10,0,10,0" VerticalAlignment="Center" />
                                    </StackPanel>

                                    <!-- Mod Name & Version -->
                                    <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                        <TextBlock Text="{Binding Name}" FontSize="14" FontWeight="SemiBold" Foreground="{DynamicResource TextFillColorPrimaryBrush}" />
                                        <StackPanel Orientation="Horizontal" Margin="0,2,0,0">
                                            <TextBlock Text="{Binding Version, StringFormat='Version: \{0\}'}" FontSize="11" Foreground="{DynamicResource TextFillColorSecondaryBrush}" />
                                            <TextBlock Text="{Binding ConflictStatus, StringFormat='  •  \{0\}'}" FontSize="11" Foreground="#FFD83B01" FontWeight="SemiBold" Visibility="{Binding HasConflict, Converter={StaticResource BooleanToVisibilityConverter}}" />
                                        </StackPanel>
                                    </StackPanel>

                                    <!-- Tags -->
                                    <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center">
                                        <!-- Type Tag (Asset, ASI, Script) -->
                                        <Border x:Name="TypeTagBorder" CornerRadius="4" Padding="6,2" Margin="5,0" VerticalAlignment="Center">
                                            <TextBlock x:Name="TypeTagText" FontSize="10" FontWeight="Bold" Foreground="White" />
                                        </Border>

                                        <Border x:Name="CompatBorder" CornerRadius="4" Padding="6,2" Margin="5,0" VerticalAlignment="Center" Background="{DynamicResource ControlFillColorDefaultBrush}">
                                            <TextBlock Text="{Binding Compatibility}" FontSize="10" FontWeight="Bold" Foreground="{DynamicResource TextFillColorPrimaryBrush}" />
                                        </Border>
                                    </StackPanel>
                                </Grid>
                            </Border>
                            <DataTemplate.Triggers>
                                <!-- Map Target tags -->
                                <DataTrigger Binding="{Binding TypeTag}" Value="Update">
                                    <Setter TargetName="TypeTagBorder" Property="Background" Value="#FF107C41" />
                                    <Setter TargetName="TypeTagText" Property="Text" Value="ASSET" />
                                </DataTrigger>
                                <DataTrigger Binding="{Binding TypeTag}" Value="Plugins">
                                    <Setter TargetName="TypeTagBorder" Property="Background" Value="#FF0078D4" />
                                    <Setter TargetName="TypeTagText" Property="Text" Value="ASI" />
                                </DataTrigger>
                                <DataTrigger Binding="{Binding TypeTag}" Value="Scripts">
                                    <Setter TargetName="TypeTagBorder" Property="Background" Value="#FFD83B01" />
                                    <Setter TargetName="TypeTagText" Property="Text" Value="SCRIPT" />
                                </DataTrigger>

                                <!-- Highlight compatibility status -->
                                <DataTrigger Binding="{Binding Compatibility}" Value="CE-compatible">
                                    <Setter TargetName="CompatBorder" Property="Background" Value="#FF107C41" />
                                </DataTrigger>
                                <DataTrigger Binding="{Binding Compatibility}" Value="Legacy">
                                    <Setter TargetName="CompatBorder" Property="Background" Value="#FF8A0A0A" />
                                </DataTrigger>
                                <DataTrigger Binding="{Binding Compatibility}" Value="Mixed">
                                    <Setter TargetName="CompatBorder" Property="Background" Value="#FFD83B01" />
                                </DataTrigger>
                            </DataTemplate.Triggers>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </ui:Card>

            <!-- Inspector panel for selected Mod details -->
            <ui:Card Grid.Column="1" Margin="10,0,0,0" Padding="15">
                <Grid>
                    <!-- Placeholder when null -->
                    <TextBlock Text="Select a mod to view and edit details." 
                               HorizontalAlignment="Center" 
                               VerticalAlignment="Center" 
                               Foreground="{DynamicResource TextFillColorSecondaryBrush}" 
                               FontSize="14">
                        <TextBlock.Style>
                            <Style TargetType="TextBlock">
                                <Setter Property="Visibility" Value="Collapsed" />
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding SelectedItem, ElementName=LibraryListBox}" Value="{x:Null}">
                                        <Setter Property="Visibility" Value="Visible" />
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </TextBlock.Style>
                    </TextBlock>

                    <!-- Details Editor when NOT null -->
                    <Grid DataContext="{Binding SelectedItem, ElementName=LibraryListBox}">
                        <Grid.Style>
                            <Style TargetType="Grid">
                                <Setter Property="Visibility" Value="Visible" />
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding SelectedItem, ElementName=LibraryListBox}" Value="{x:Null}">
                                        <Setter Property="Visibility" Value="Collapsed" />
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Grid.Style>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="*" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>

                        <ScrollViewer Grid.Row="0" VerticalScrollBarVisibility="Auto">
                            <StackPanel>
                                <TextBlock Text="Edit Mod Details" FontSize="18" FontWeight="Bold" Margin="0,0,0,15" Foreground="{DynamicResource TextFillColorPrimaryBrush}" />

                                <TextBlock Text="Name" FontSize="11" Foreground="{DynamicResource TextFillColorSecondaryBrush}" Margin="0,0,0,4" />
                                <ui:TextBox Text="{Binding Name, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,12" />

                                <TextBlock Text="Version" FontSize="11" Foreground="{DynamicResource TextFillColorSecondaryBrush}" Margin="0,0,0,4" />
                                <ui:TextBox Text="{Binding Version, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,12" />

                                <TextBlock Text="Compatibility" FontSize="11" Foreground="{DynamicResource TextFillColorSecondaryBrush}" Margin="0,0,0,4" />
                                <ui:TextBox Text="{Binding Compatibility, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,12" />

                                <TextBlock Text="Description" FontSize="11" Foreground="{DynamicResource TextFillColorSecondaryBrush}" Margin="0,0,0,4" />
                                <ui:TextBox Text="{Binding Description, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" 
                                            AcceptsReturn="True" 
                                            TextWrapping="Wrap" 
                                            MinLines="3" 
                                            Margin="0,0,0,12" />

                                <TextBlock Text="File Layout (Manifest)" FontSize="11" Foreground="{DynamicResource TextFillColorSecondaryBrush}" Margin="0,0,0,4" />
                                <ListBox ItemsSource="{Binding Model.Files}" MaxHeight="150" Background="{DynamicResource ControlFillColorDefaultBrush}" BorderBrush="{DynamicResource ControlStrokeColorDefaultBrush}" BorderThickness="1" Margin="0,0,0,15">
                                    <ListBox.ItemTemplate>
                                        <DataTemplate>
                                            <TextBlock Text="{Binding RelativePath}" FontSize="11" Foreground="{DynamicResource TextFillColorSecondaryBrush}" />
                                        </DataTemplate>
                                    </ListBox.ItemTemplate>
                                </ListBox>
                            </StackPanel>
                        </ScrollViewer>

                        <!-- Action Buttons at the bottom -->
                        <Grid Grid.Row="1" Margin="0,10,0,0">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            
                            <ui:Button Grid.Column="0"
                                       Content="Delete Mod" 
                                       Icon="{ui:SymbolIcon Delete24}" 
                                       Command="{Binding DataContext.DeleteModCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" 
                                       CommandParameter="{Binding}"
                                       Appearance="Danger" 
                                       HorizontalAlignment="Left" />
                                       
                            <ui:Button Grid.Column="1" 
                                       Content="Save Details" 
                                       Icon="{ui:SymbolIcon Save24}" 
                                       Command="{Binding DataContext.SaveModDetailsCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" 
                                       Appearance="Primary" 
                                       HorizontalAlignment="Right" />
                        </Grid>
                    </Grid>
                </Grid>
            </ui:Card>
        </Grid>
    </Grid>
</UserControl>
