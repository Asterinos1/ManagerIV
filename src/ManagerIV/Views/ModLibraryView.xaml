<UserControl x:Class="ManagerIV.Views.ModLibraryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             xmlns:local="clr-namespace:ManagerIV.Views"
             xmlns:vm="clr-namespace:ManagerIV.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800"
             AllowDrop="True"
             Drop="UserControl_Drop"
             DragOver="UserControl_DragOver">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />
    </UserControl.Resources>
    
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Header and drag-and-drop info -->
        <Grid Grid.Row="0" Margin="0,0,0,15">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <StackPanel Grid.Column="0">
                <TextBlock Text="Mod Library" FontSize="24" FontWeight="Bold" Foreground="White" />
                <TextBlock Text="Drag and drop mod archives (.zip, .rar, .7z) here to import them." FontSize="13" Foreground="#FFAAAAAA" Margin="0,4,0,0" />
            </StackPanel>
            
            <ui:Button Grid.Column="1" 
                       Icon="{ui:SymbolIcon FolderAdd24}" 
                       Content="Import Archive" 
                       Command="{Binding ImportModArchiveCommand}"
                       VerticalAlignment="Center" />
        </Grid>

        <!-- Library List with Drag-and-Drop Reordering -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="3*" />
                <ColumnDefinition Width="2*" />
            </Grid.ColumnDefinitions>

            <ui:Card Grid.Column="0" Margin="0,0,10,0" Padding="0">
                <ListBox x:Name="LibraryListBox" ItemsSource="{Binding LibraryMods}"
                         ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                         PreviewMouseLeftButtonDown="ListBox_PreviewMouseLeftButtonDown"
                         MouseMove="ListBox_MouseMove"
                         Drop="ListBox_Drop"
                         AllowDrop="True"
                         Background="Transparent"
                         BorderThickness="0">
                    <ListBox.ItemTemplate>
                        <DataTemplate DataType="{x:Type vm:ModViewModel}">
                            <Border BorderThickness="0,0,0,1" BorderBrush="#FF2D2D2D" Padding="10,8" Margin="0" HorizontalAlignment="Stretch">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto" />  <!-- Priority / Enable Checkbox -->
                                        <ColumnDefinition Width="*" />     <!-- Mod Info (Name, Version) -->
                                        <ColumnDefinition Width="Auto" />  <!-- Visual Tags (Type, Compatibility) -->
                                    </Grid.ColumnDefinitions>

                                    <!-- Priority Indicator and Toggle Checkbox -->
                                    <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                                        <TextBlock Text="{Binding Priority, StringFormat='\{0:D3\}'}" FontSize="12" Foreground="#FF888888" Width="30" VerticalAlignment="Center" TextAlignment="Center" />
                                        <CheckBox IsChecked="{Binding IsEnabled}" Command="{Binding DataContext.ToggleModEnabledCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" CommandParameter="{Binding}" Margin="10,0,10,0" VerticalAlignment="Center" />
                                    </StackPanel>

                                    <!-- Mod Name & Version -->
                                    <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                        <TextBlock Text="{Binding Name}" FontSize="14" FontWeight="SemiBold" Foreground="White" />
                                        <StackPanel Orientation="Horizontal" Margin="0,2,0,0">
                                            <TextBlock Text="{Binding Version, StringFormat='Version: \{0\}'}" FontSize="11" Foreground="#FFAAAAAA" />
                                            <TextBlock Text="{Binding ConflictStatus, StringFormat='  •  \{0\}'}" FontSize="11" Foreground="#FFD83B01" FontWeight="SemiBold" Visibility="{Binding HasConflict, Converter={StaticResource BooleanToVisibilityConverter}}" />
                                        </StackPanel>
                                    </StackPanel>

                                    <!-- Tags -->
                                    <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center">
                                        <!-- Type Tag (Asset, ASI, Script) -->
                                        <Border x:Name="TypeTagBorder" CornerRadius="4" Padding="6,2" Margin="5,0" VerticalAlignment="Center">
                                            <TextBlock x:Name="TypeTagText" FontSize="10" FontWeight="Bold" Foreground="White" />
                                        </Border>

                                        <!-- Compatibility Status Badge -->
                                        <Border x:Name="CompatBorder" CornerRadius="4" Padding="6,2" Margin="5,0" VerticalAlignment="Center" Background="#FF333333">
                                            <TextBlock Text="{Binding Compatibility}" FontSize="10" FontWeight="Bold" Foreground="White" />
                                        </Border>
                                    </StackPanel>
                                </Grid>
                            </Border>
                            <DataTemplate.Triggers>
                                <!-- Map Target tags -->
                                <DataTrigger Binding="{Binding TypeTag}" Value="Update">
                                    <Setter TargetName="TypeTagBorder" Property="Background" Value="#FF107C41" />
                                    <Setter TargetName="TypeTagText" Property="Text" Value="ASSET" />
                                </DataTrigger>
                                <DataTrigger Binding="{Binding TypeTag}" Value="Plugins">
                                    <Setter TargetName="TypeTagBorder" Property="Background" Value="#FF0078D4" />
                                    <Setter TargetName="TypeTagText" Property="Text" Value="ASI" />
                                </DataTrigger>
                                <DataTrigger Binding="{Binding TypeTag}" Value="Scripts">
                                    <Setter TargetName="TypeTagBorder" Property="Background" Value="#FFD83B01" />
                                    <Setter TargetName="TypeTagText" Property="Text" Value="SCRIPT" />
                                </DataTrigger>

                                <!-- Highlight compatibility status -->
                                <DataTrigger Binding="{Binding Compatibility}" Value="CE-compatible">
                                    <Setter TargetName="CompatBorder" Property="Background" Value="#FF107C41" />
                                </DataTrigger>
                                <DataTrigger Binding="{Binding Compatibility}" Value="Legacy">
                                    <Setter TargetName="CompatBorder" Property="Background" Value="#FF8A0A0A" />
                                </DataTrigger>
                                <DataTrigger Binding="{Binding Compatibility}" Value="Mixed">
                                    <Setter TargetName="CompatBorder" Property="Background" Value="#FFD83B01" />
                                </DataTrigger>
                            </DataTemplate.Triggers>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </ui:Card>

            <!-- Inspector panel for selected Mod details -->
            <ui:Card Grid.Column="1" Margin="10,0,0,0" Padding="15">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>
                    
                    <ScrollViewer Grid.Row="0">
                        <StackPanel>
                            <TextBlock Text="Mod Details" FontSize="18" FontWeight="Bold" Margin="0,0,0,15" Foreground="White" />
                            
                            <TextBlock Text="Name" FontSize="11" Foreground="#FFAAAAAA" />
                            <TextBlock Text="{Binding SelectedItem.Name, ElementName=LibraryListBox, FallbackValue='No mod selected'}" FontSize="15" FontWeight="SemiBold" Foreground="White" Margin="0,2,0,12" TextWrapping="Wrap" />
                            
                            <TextBlock Text="Version" FontSize="11" Foreground="#FFAAAAAA" />
                            <TextBlock Text="{Binding SelectedItem.Version, ElementName=LibraryListBox, FallbackValue='N/A'}" FontSize="13" Foreground="White" Margin="0,2,0,12" />

                            <TextBlock Text="Compatibility Rating" FontSize="11" Foreground="#FFAAAAAA" />
                            <TextBlock Text="{Binding SelectedItem.Compatibility, ElementName=LibraryListBox, FallbackValue='N/A'}" FontSize="13" Foreground="White" Margin="0,2,0,12" />

                            <TextBlock Text="Description" FontSize="11" Foreground="#FFAAAAAA" />
                            <TextBlock Text="{Binding SelectedItem.Model.Description, ElementName=LibraryListBox, FallbackValue='Select a mod to view its details.'}" FontSize="13" Foreground="White" Margin="0,2,0,12" TextWrapping="Wrap" />

                            <TextBlock Text="File Layout (Manifest)" FontSize="11" Foreground="#FFAAAAAA" Margin="0,0,0,4" />
                            <ListBox ItemsSource="{Binding SelectedItem.Model.Files, ElementName=LibraryListBox}" MaxHeight="200" Background="#FF1E1E1E" BorderBrush="#FF333333" BorderThickness="1">
                                <ListBox.ItemTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding RelativePath}" FontSize="11" Foreground="#FFAAAAAA" />
                                    </DataTemplate>
                                </ListBox.ItemTemplate>
                            </ListBox>
                        </StackPanel>
                    </ScrollViewer>
                </Grid>
            </ui:Card>
        </Grid>
    </Grid>
</UserControl>
