<UserControl x:Class="ManagerIV.Views.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             xmlns:local="clr-namespace:ManagerIV.Views"
             xmlns:vm="clr-namespace:ManagerIV.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800"
             Background="{DynamicResource SolidBackgroundFillColorBaseBrush}">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Margin="0,0,0,20">
            <TextBlock Text="Settings" FontSize="24" FontWeight="Bold" Foreground="{DynamicResource TextFillColorPrimaryBrush}" />
            <TextBlock Text="Configure global application settings, themes, and aesthetics." FontSize="13" Foreground="{DynamicResource TextFillColorSecondaryBrush}" Margin="0,4,0,0" TextWrapping="Wrap" />
        </StackPanel>

        <!-- Settings Content -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
            <StackPanel MaxWidth="600" HorizontalAlignment="Left" Margin="0,0,15,0">


                <!-- GPU VRAM Settings Card -->
                <ui:Card Padding="15" Margin="0,0,0,15">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        
                        <StackPanel Grid.Row="0" Grid.Column="0" VerticalAlignment="Center" Margin="0,0,15,0">
                            <TextBlock Text="GPU VRAM Target (MB)" FontSize="14" FontWeight="SemiBold" Foreground="{DynamicResource TextFillColorPrimaryBrush}" />
                            <TextBlock Text="Set VRAM size reported to GTA IV to fix the VRAM detection bug (default: 2048)." FontSize="11" Foreground="{DynamicResource TextFillColorSecondaryBrush}" Margin="0,2,0,0" />
                        </StackPanel>
                        
                        <ui:TextBox Grid.Row="0" Grid.Column="1" 
                                    Text="{Binding GpuVramMb, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" 
                                    Width="120" 
                                    VerticalAlignment="Center"
                                    PlaceholderText="2048" />

                        <!-- Quick select presets -->
                        <StackPanel Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="2" Orientation="Horizontal" Margin="0,12,0,0">
                            <TextBlock Text="Presets:" VerticalAlignment="Center" FontSize="11" Foreground="{DynamicResource TextFillColorSecondaryBrush}" Margin="0,0,8,0" />
                            <ui:Button Content="2 GB" Command="{Binding SetVramPresetCommand}" CommandParameter="2048" Margin="0,0,5,0" Padding="10,4" FontSize="11" />
                            <ui:Button Content="4 GB" Command="{Binding SetVramPresetCommand}" CommandParameter="4096" Margin="0,0,5,0" Padding="10,4" FontSize="11" />
                            <ui:Button Content="6 GB" Command="{Binding SetVramPresetCommand}" CommandParameter="6144" Margin="0,0,5,0" Padding="10,4" FontSize="11" />
                            <ui:Button Content="8 GB" Command="{Binding SetVramPresetCommand}" CommandParameter="8192" Margin="0,0,5,0" Padding="10,4" FontSize="11" />
                            <ui:Button Content="10 GB" Command="{Binding SetVramPresetCommand}" CommandParameter="10240" Margin="0,0,5,0" Padding="10,4" FontSize="11" />
                        </StackPanel>
                    </Grid>
                </ui:Card>

                <!-- Save Profiles Path Card -->
                <ui:Card Padding="15" Margin="0,0,0,15">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>

                        <StackPanel Grid.Row="0" Grid.Column="0" VerticalAlignment="Center" Margin="0,0,15,0">
                            <TextBlock Text="GTA IV Save Profiles Location" FontSize="14" FontWeight="SemiBold" Foreground="{DynamicResource TextFillColorPrimaryBrush}" />
                            <TextBlock Text="Path to Rockstar Games Profiles folder containing save slots (defaults to Documents\Rockstar Games\GTA IV\Profiles)." FontSize="11" Foreground="{DynamicResource TextFillColorSecondaryBrush}" Margin="0,2,0,0" />
                        </StackPanel>

                        <ui:Button Grid.Row="0" Grid.Column="1" 
                                   Content="Browse..." 
                                   Icon="{ui:SymbolIcon Folder24}" 
                                   Command="{Binding BrowseSaveProfilesPathCommand}" 
                                   VerticalAlignment="Center" />

                        <TextBlock Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="2" 
                                   Text="{Binding GtaSaveProfilesPath, FallbackValue='Default path (using user Documents)'}" 
                                   FontSize="12" 
                                   Foreground="{DynamicResource TextFillColorPrimaryBrush}" 
                                   Margin="0,10,0,0" 
                                   TextWrapping="Wrap" />
                    </Grid>
                </ui:Card>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>
