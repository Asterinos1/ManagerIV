<Window x:Class="ManagerIV.Views.LibertyTrainerInstallDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        mc:Ignorable="d"
        Title="Liberty's Legacy Installation Assistant"
        Height="440" Width="580"
        MinWidth="520" MinHeight="400"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="{DynamicResource ApplicationBackgroundBrush}"
        Foreground="{DynamicResource TextFillColorPrimaryBrush}">
    
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />  <!-- Header -->
            <RowDefinition Height="*" />     <!-- Body & Info -->
            <RowDefinition Height="Auto" />  <!-- Status & Progress -->
            <RowDefinition Height="Auto" />  <!-- Buttons -->
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Margin="0,0,0,16">
            <StackPanel Orientation="Horizontal">
                <ui:SymbolIcon Symbol="ArrowDownload24" Filled="True" Foreground="{DynamicResource SystemAccentColorSecondaryBrush}" FontSize="24" Margin="0,0,10,0" VerticalAlignment="Center" />
                <TextBlock Text="Liberty's Legacy Installation" FontSize="20" FontWeight="Bold" Foreground="{DynamicResource TextFillColorPrimaryBrush}" VerticalAlignment="Center" />
            </StackPanel>
            <TextBlock Text="Follow the steps below to complete the trainer installation." FontSize="12" Foreground="{DynamicResource TextFillColorSecondaryBrush}" Margin="0,4,0,0" />
        </StackPanel>

        <!-- Body & Info -->
        <StackPanel Grid.Row="1" Margin="0,0,0,16">
            <!-- Instruction Card -->
            <ui:Card Padding="14" Margin="0,0,0,12">
                <StackPanel>
                    <TextBlock Text="1. The official GTAForums release topic by const96b has opened in your browser." FontSize="12" Foreground="{DynamicResource TextFillColorPrimaryBrush}" Margin="0,0,0,6" TextWrapping="Wrap" />
                    <TextBlock Text="2. Download the trainer ZIP file into your normal Downloads folder." FontSize="12" Foreground="{DynamicResource TextFillColorPrimaryBrush}" Margin="0,0,0,6" TextWrapping="Wrap" />
                    <TextBlock Text="3. Do NOT extract the ZIP. ManagerIV will automatically detect it, validate its contents, and deploy it." FontSize="12" Foreground="{DynamicResource SystemAccentColorSecondaryBrush}" FontWeight="SemiBold" TextWrapping="Wrap" />
                </StackPanel>
            </ui:Card>

            <!-- Monitored Directory Card -->
            <ui:CardControl Icon="{ui:SymbolIcon Folder24}">
                <ui:CardControl.Header>
                    <StackPanel>
                        <TextBlock Text="Monitored Downloads Directory" FontSize="13" FontWeight="SemiBold" Foreground="{DynamicResource TextFillColorPrimaryBrush}" />
                        <TextBlock x:Name="DownloadsPathText" Text="C:\Users\...\Downloads" FontSize="11" Foreground="{DynamicResource TextFillColorSecondaryBrush}" Margin="0,2,0,0" TextTrimming="CharacterEllipsis" />
                    </StackPanel>
                </ui:CardControl.Header>
            </ui:CardControl>
        </StackPanel>

        <!-- Status & Progress -->
        <Border Grid.Row="2" CornerRadius="8" Background="{DynamicResource ControlFillColorDefaultBrush}" BorderBrush="{DynamicResource ControlStrokeColorDefaultBrush}" BorderThickness="1" Padding="14" Margin="0,0,0,16">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <ui:ProgressRing Grid.Column="0" IsIndeterminate="True" Width="24" Height="24" Margin="0,0,12,0" VerticalAlignment="Center" />
                <StackPanel Grid.Column="1" VerticalAlignment="Center">
                    <TextBlock x:Name="StatusMessageText" Text="Waiting for Liberty's Legacy download..." FontSize="13" FontWeight="SemiBold" Foreground="{DynamicResource TextFillColorPrimaryBrush}" TextWrapping="Wrap" />
                    <TextBlock Text="Monitoring for completed ZIP archive..." FontSize="11" Foreground="{DynamicResource TextFillColorSecondaryBrush}" Margin="0,2,0,0" />
                </StackPanel>
            </Grid>
        </Border>

        <!-- Actions -->
        <Grid Grid.Row="3">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <ui:Button Grid.Column="0" 
                       x:Name="SelectZipButton" 
                       Content="Select ZIP..." 
                       Icon="{ui:SymbolIcon FolderOpen24}" 
                       Click="SelectZipButton_Click" 
                       Appearance="Secondary" />

            <ui:Button Grid.Column="2" 
                       x:Name="CancelButton" 
                       Content="Cancel" 
                       Click="CancelButton_Click" 
                       Appearance="Secondary" />
        </Grid>
    </Grid>
</Window>
